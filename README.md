A simple, native, cross-platform notification enabled app.  
Push notifications are provided by OneSignal SDKs. 
The notification pushes the user to a full-screen web view with custom content.
 
  - Android
  - iOS
